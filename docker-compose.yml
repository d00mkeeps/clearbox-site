services:
  clearbox-site:
    build: .
    container_name: clearbox-site
    restart: unless-stopped
    ports:
      - "8082:80"
    networks:
      - medicine-network

networks:
  medicine-network:
    external: true